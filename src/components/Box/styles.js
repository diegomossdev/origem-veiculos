import styled from 'styled-components';
import { colors } from '../../styles/colors';

export const Container = styled.div`
  padding: 30px;
  background: ${colors.admin.white};
  border-radius: 4px;
  border: 1px solid ${colors.admin.border.normal};
`;
